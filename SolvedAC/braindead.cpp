using namespace std;
#include <bits/stdc++.h>
#define ll long long

int main()
{
    ll a, b; cin >> a >> b;
    double res = (double) a/ (double) b;
    cout << setprecision(15); 

    cout << fixed <<  res << "\n";
}