using namespace std;
#include <iostream>
#include <vector>
#include <algorithm>
#include <cstdio>
#include <map>
#include <set>
#include <queue>
#include <string>
#include <math.h>
int N;
lon
long long int find_shortest(long long int a, long long int b)
{
    dictionary = {};
    long long int shortest = 1000000000;
    while(true)
    {   
        
    }
}
int main()
{
    cin >> N;
    for(int i = 0; i < N; i++)
    {
        long long int a, b;
        cin >> a >> b;
        long long int res = find_shortest(a, b);
    }
}